import { render } from "@testing-library/react";
import { Home } from "./index";
describe("login content", () => {
  test("El login", () => {
    render(<Home />);
    //obtener el elemento al que se quiere hacer testing
    //determinar cual es la prueva a realizar
  });
});
